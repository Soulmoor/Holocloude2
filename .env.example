# =============================================================================
# HOLOCLOUDE UMGEBUNGSVARIABLEN
# =============================================================================
# Kopiere diese Datei nach .env und passe die Werte an:
#   cp .env.example .env
#
# WICHTIG: Die .env Datei sollte NIEMALS committed werden!
# =============================================================================

# -----------------------------------------------------------------------------
# MQTT KONFIGURATION (fuer holo_device_agent.py)
# -----------------------------------------------------------------------------
HOLO_MQTT_BROKER=localhost
HOLO_MQTT_PORT=1883
HOLO_MQTT_USER=
HOLO_MQTT_PASSWORD=

# -----------------------------------------------------------------------------
# RAM MANAGER KONFIGURATION (fuer holo_ram_manager.py)
# -----------------------------------------------------------------------------
# RAM-Cache Limit in MB (default: 500)
HOLO_RAM_CACHE_MB=500

# SD-Speicher Limit in GB (default: 16)
HOLO_SD_STORAGE_GB=16

# -----------------------------------------------------------------------------
# DISCORD BOT (falls verwendet)
# -----------------------------------------------------------------------------
DISCORD_BOT_TOKEN=
DISCORD_GUILD_ID=

# -----------------------------------------------------------------------------
# LLM KONFIGURATION (fuer smart_llm_system.py)
# -----------------------------------------------------------------------------
# Lokaler LLM Server (z.B. Ollama)
LLM_LOCAL_URL=http://localhost:11434

# OpenAI API (optional, als Fallback)
OPENAI_API_KEY=

# Anthropic API (optional, als Fallback)
ANTHROPIC_API_KEY=

# -----------------------------------------------------------------------------
# SMART HOME / IOT
# -----------------------------------------------------------------------------
# Home Assistant
HOME_ASSISTANT_URL=
HOME_ASSISTANT_TOKEN=

# -----------------------------------------------------------------------------
# DATENBANK
# -----------------------------------------------------------------------------
# SQLite Pfad (default: data/)
HOLO_DATA_PATH=data/

# -----------------------------------------------------------------------------
# LOGGING
# -----------------------------------------------------------------------------
# Log Level: DEBUG, INFO, WARNING, ERROR, CRITICAL
HOLO_LOG_LEVEL=INFO

# Log Pfad (default: logs/)
HOLO_LOG_PATH=logs/

# -----------------------------------------------------------------------------
# SICHERHEIT
# -----------------------------------------------------------------------------
# Geheimer Schluessel fuer Session-Tokens (generiere mit: python -c "import secrets; print(secrets.token_hex(32))")
HOLO_SECRET_KEY=

# -----------------------------------------------------------------------------
# ENTWICKLUNG
# -----------------------------------------------------------------------------
# Debug Modus aktivieren (True/False)
HOLO_DEBUG=False

# Profiling aktivieren
HOLO_PROFILING=False
